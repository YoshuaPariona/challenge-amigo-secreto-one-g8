import { renderForm } from "./presentation/render-form.js";

const form = document.querySelector('.name-form');
const name = document.querySelector('.name-cards');


renderForm( form );
renderName( name )